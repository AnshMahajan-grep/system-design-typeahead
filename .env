# Credentials used by docker-compose. All local-only / demo values — there are
# no real secrets here, which is why this file is safe to commit for an
# assignment. Change them if you like.
POSTGRES_USER=typeahead
POSTGRES_PASSWORD=typeahead
POSTGRES_DB=typeahead
