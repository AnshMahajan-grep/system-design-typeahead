{
  "name": "typeahead-backend",
  "version": "1.0.0",
  "description": "Search typeahead backend: Express + Postgres + distributed Redis cache (consistent hashing) + batch writes + recency-aware trending",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "seed": "node scripts/seed.js",
    "benchmark": "node scripts/benchmark.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0"
  }
}
